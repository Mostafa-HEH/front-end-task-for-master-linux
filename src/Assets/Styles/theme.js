import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  // chaneg main styles from here but because of that the task main is about development
  // so, i will leave it as default styles
});
